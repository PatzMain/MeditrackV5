{
  "name": "meditrack",
  "version": "1.0.0",
  "description": "Patient Monitoring and Inventory Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"PORT=3000 npm start --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}